import 'package:flutter/material.dart';

class ScopeSpaceModelWidget {
  final String? scope;
  final dynamic icon;
  final Color? color;

  const ScopeSpaceModelWidget({this.scope, this.icon, this.color});
}
